import { createBlockRenderer } from '../utils/create-block-renderer';
export default createBlockRenderer('bulleted_list', async (data, renderer) => {
    return `<ul class="notion-${data.type}">${await renderer.render(...data.bulleted_list)}</ul>`;
});
//# sourceMappingURL=bulleted-list-block.renderer.js.map