import { createBlockRenderer } from '../utils/create-block-renderer';
export default createBlockRenderer('to_do_list', async (data, renderer) => {
    return `<ul class="notion-${data.type}">${await renderer.render(...data.to_do_list)}</ul>`;
});
//# sourceMappingURL=to-do-list-block.renderer.js.map