import { createBlockRenderer } from '../utils/create-block-renderer';
export default createBlockRenderer('bulleted_list_item', async (data, renderer) => {
    return `
            <li class="notion-${data.type} notion-color-${data.bulleted_list_item.color}">
                ${await renderer.render(...data.bulleted_list_item.rich_text)}
            </li>
        `;
});
//# sourceMappingURL=bulleted-list-item-block.renderer.js.map