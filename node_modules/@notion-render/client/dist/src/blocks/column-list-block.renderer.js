import { createBlockRenderer } from '../utils/create-block-renderer';
export default createBlockRenderer('column_list', async (data, renderer) => {
    if (!renderer.client || !data.has_children)
        return '';
    return `
            <div class="notion-${data.type}">
                ${await renderer.renderBlock(data.id)}
            </div>
        `;
});
//# sourceMappingURL=column-list-block.renderer.js.map