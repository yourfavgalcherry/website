import { createBlockRenderer } from '../utils/create-block-renderer';
export default createBlockRenderer('numbered_list_item', async (data, renderer) => {
    return `
            <li class="notion-${data.type} notion-color-${data.numbered_list_item.color}">
                ${await renderer.render(...data.numbered_list_item.rich_text)}
            </li>
        `;
});
//# sourceMappingURL=numbered-list-item-block.renderer.js.map